import styles from 'component.module.scss'
import React from "react";
import {PersonalizedNewSong} from "@/models/personalized";

const Component: React.FC<PersonalizedNewSong> = (props) => {
    console.log(props)
    return <div>

    </div>
}

export default Component